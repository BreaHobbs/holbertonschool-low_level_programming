#include "holberton.h"
/**
* add - function that adds two results together
* @x: takes in input into function
* @y: takes in other input into function
* Return: returns successfully a 0 upon completion
*/
int add(int x, int y)
{
	return (x + y);
}
